% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_functions.R
\name{mergeomics}
\alias{mergeomics}
\title{Merge multi-omics datasets}
\usage{
mergeomics(
  enviro = NULL,
  pheno = NULL,
  geno = NULL,
  by.env = "site_id",
  by.pheno = c("site_id", "tree_id"),
  by.geno = c("site_id", "tree_id"),
  join.type = c("inner", "left", "full")
)
}
\arguments{
\item{enviro}{Environmental data frame (optional)}

\item{pheno}{Phenotypic data frame (optional)}

\item{geno}{Genotypic data frame (optional)}

\item{by.env}{Character vector of columns to join environmental data (default: "site_id")}

\item{by.pheno}{Character vector of columns to join phenotypic data (default: c("site_id", "tree_id"))}

\item{by.geno}{Character vector of columns to join genotypic data (default: c("site_id", "tree_id"))}

\item{join.type}{Type of join: "inner", "left", "full" (default: "inner")}
}
\value{
A merged data frame
}
\description{
Intelligently merge environmental, phenotypic, and genotypic data with
flexible join keys and options.
}
\examples{
env <- envirodata(n_sites = 5)
pheno <- phenotydata(n_trees = 25)
geno <- genotydata(n_trees = 25)
merged <- mergeomics(enviro = env, pheno = pheno, geno = geno)
}
